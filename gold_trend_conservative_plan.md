# 黄金顺势保守策略草案（执行版）

## 1. 目标与硬约束

- 目标：低回撤、稳健盈利
- 品种：`XAUUSD`
- 账户：美分账户
- 单轮最大亏损：`1000` 美分（硬止损）
- 原则：先控风险，再要利润

---

## 2. 模块划分（严格区分）

### 2.1 模块A：`L16` 保险单（保持不变）

- 触发：主序列首次到达 `L16`
- 性质：结构触发
- 作用：极端行情下提供保险现金流
- 备注：本模块逻辑不改，但执行参数明确如下
- `L16` 保险单分批：`0.01 -> 0.02 -> 0.03`（同方向条件仍有效时逐笔触发）
- 单次触发间隔：`0.5-1.0` 秒（仅在“上一笔反向仓已平仓”后开始计时）
- 最大保险单批次数：`3` 批

### 2.2 模块B：动态反向仓（少量使用）

- 触发：条件触发，不绑定固定层级
- 起始：从主序列第 `4` 笔开始允许触发
- 性质：战术降压，短打快出
- 原则：低频、小手数、只在必要时使用

---

## 3. 主序列开仓规则（你确认版）

### 3.1 主方向优先

- 以主方向开仓为核心（你当前偏向“开始买单为主”）
- 动态反向仓是辅助，不是主轴

### 3.2 前两笔双发（硬规则）

- 第1笔：下 `2` 单，每单 `0.01`
- 第2笔：下 `2` 单，每单 `0.02`

### 3.3 第3笔以后按层级手数

| 层级 | 单笔手数 | 下单数量 |
| --- | --- | --- |
| 3 | 0.03 | 1 |
| 4 | 0.04 | 1 |
| 5 | 0.05 | 1 |
| 6 | 0.05 | 1 |
| 7 | 0.06 | 1 |
| 8 | 0.07 | 1 |
| 9 | 0.09 | 1 |
| 10 | 0.11 | 1 |
| 11 | 0.11 | 1 |
| 12 | 0.11 | 1 |
| 13 | 0.13 | 1 |
| 14 | 0.13 | 1 |
| 15 | 0.15 | 1 |
| 16 | 0.18 | 1 |
| 17 | 0.20 | 1 |
| 18 | 0.22 | 1 |

说明：  
如果后续你希望第3-6笔也做双发，可以单独开一个“进攻档位”，不要和当前基础档混用。

---

## 4. `L16` 结束后重启规则

- 当 `L16` 相关仓位完成平仓后，允许立即再次下仓
- 不设置长等待，不强制冷却
- 仅保留极短防重入延时：`0.3-1.0` 秒
- 增加状态锁：若当前轮次仍在“分步平仓循环”中，禁止重启开仓
- 仅当状态从 `CLOSING` 切换到 `IDLE` 后才允许立即重启

### 4.1 `L16` 应急重入规则（新增）

- 适用场景：到达 `L16` 后，反向仓已平完，且主方向条件仍成立。
- 核心动作：允许“快速继续买入”（重入主方向），但必须带护栏。

触发前检查（全部满足才允许）：

- `spread_guard` 通过（点差正常）
- `margin_level >= 350%`
- 当前轮次未触发硬止损警戒
- 状态已回到 `IDLE`

重入约束：

- 防重入延时：`0.3-1.0` 秒
- 单次应急重入手数：不超过当前主净敞口的 `10%-15%`
- 单个 `L16` 事件最多应急重入 `2-3` 次
- 若连续两次重入后无风险改善，则停止重入，转回减仓/退出

---

## 5. 动态反向仓触发条件（从第4笔开始）

以下条件全部满足时才允许开：

- 主序列已到 `L4` 或更深
- 短时波动达到阈值（建议 `1.2-1.5 * ATR`）
- 高周期趋势仍对主仓不利
- 点差和成交质量正常
- 总对冲暴露未超上限

执行建议：

- 单笔手数：主净敞口的 `5%-10%`
- 每轮最多触发 `1-2` 次
- 触发节奏：不做高频连续触发，反向仓平仓后需满足下一次条件再触发
- 盈利优先用于削减最危险主仓
- 禁止滚成第二套重仓系统

---

## 6. 对冲总暴露上限（A+B合并）

- 模块A（`L16` 保险单）+ 模块B（动态反向仓）合计反向净敞口
- 上限：不超过主序列净敞口的 `40%`
- 超限后：禁止新增反向仓，只允许平仓/减仓

---

## 7. `L16` 平仓规律（盈利导向）

目标：  
先把可兑现利润拿到手，再逐步压缩上下价差，不做“理想化一次清仓”。

### 7.1 顺序规则

1. 先平小单  
优先平“已盈利的小手数单”和短打反向仓，先锁定现金流。

2. 再平中间单  
用已实现利润去部分平掉“中间层亏损单”，不是先硬砍最深烂单。

3. 循环收敛  
每轮执行后重算带宽与均价差，继续下一轮，直到满足退出条件。

### 7.2 分步参数（建议初值）

- `min_close_interval`: `0.5-1.0` 秒
- `partial_close_ratio`: 单次 `10%-25%` 仓量
- `max_close_steps_per_cycle`: 每轮最多 `3-6` 次
- `spread_guard`: 点差超阈值暂停分步平仓

### 7.3 `L16` 与 `L14/L15` 的利润联动平仓（新增硬规则）

- 规则目标：当 `L16` 出现亏损扩大时，用 `L14/L15` 的已实现利润去部分平掉 `L16`，逐步收敛风险带宽。
- 关键前提：只允许使用“已实现利润池”，不允许用浮盈直接计入可平仓资金。

触发逻辑：

1. 反向仓或中层仓平仓后，利润先进入 `realized_profit_pool`。  
2. 当 `L16` 浮亏超过阈值（建议：单层浮亏达到当轮预算的 `15%-20%`）时，触发利润联动减仓。  
3. 每次仅对 `L16` 做部分平仓（建议 `20%-40%`），不得一次性清空。  
4. 每次执行后重算 `L16` 风险和利润池余额，不足则等待下一次已实现利润补充。  

约束条件：

- `L14/L15` 不允许被一次性掏空，必须保留最小缓冲仓（建议至少保留原始仓量的 `40%-60%`）。  
- 若 `L16` 继续恶化且触及整轮硬止损，则直接整轮退出，不再执行联动修复。  

### 7.4 退出触发

- 篮子浮亏回落到安全阈值
- 或篮子接近保本/小盈

满足后切换“整组退出优先”。

### 7.5 失败保护

- 执行过程中若触及 `-1000` 美分，立即整轮退出
- 硬止损触发后禁止继续修复动作
- `L16` 应急重入一并停止

---

## 8. 其余风控

- `margin_level` 分级保护（硬规则）
- `margin_level < 500%`：禁止新增 `L13-L18`，仅允许 `L1-L12`
- `margin_level < 350%`：冻结所有新增仓，仅允许平仓/减仓
- `margin_level < 250%`：强制执行整轮退出
- 成功标准：大多数轮次在 `L10` 前结束
- 若 `L13-L18` 触发频率偏高，说明参数过激，需回调
- 双发后动态收敛（盈利与保本平衡）
- 若前两笔双发后，轮次浮亏超过当轮预算的 `35%`，则 `L5` 以后每层手数下调 `20%`
- 若前两笔双发后，轮次浮亏超过当轮预算的 `50%`，则禁止新增动态反向仓，只保留减仓动作
- 连续失败停机：连续 `2` 轮触发硬止损后，当日停止新开轮次

---

## 9. 盈利优先执行原则（新增）

- 先赚可兑现利润，再优化账面利润
- 任何时刻“保护本金”的优先级高于“拉高单轮收益”
- 动态反向仓盈利必须优先用于削减危险主仓，不用于扩大新仓
- 达到阶段利润后优先退出，不做过度持仓等待

建议阶段利润目标（可回测后微调）：

- 常规轮次目标：`+120 ~ +260` 美分
- 压力轮次目标：`+60 ~ +150` 美分
- 超过 `L13` 的轮次：以风险收敛退出为主，利润目标次之

---

## 10. 结论

这版最终落实了你最关心的三件事：

- 第1笔和第2笔固定双发
- `L16` 逻辑不动，平仓后可立即重启
- 动态反向仓从第4笔起可用，但保持少量低频
