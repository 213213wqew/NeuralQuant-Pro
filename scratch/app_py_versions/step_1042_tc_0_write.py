"# 实时自动更新重构任务列表\n\n- [ ] 修改 `quant_app/ui/app.py` 中 `draw_kline_img` 的行情刷新与 AI 数据直连链路\n  - [ ] 针对 M1 周期增加 `fetch_count` 自适应放大逻辑\n  - [ ] 将 AI 计算与 UI 状态标记（`latest_dir_score` 等）移至哈希过滤拦截的前方\n  - [ ] 在哈希指纹 `current_state_hash` 中并入 `len(df)` 防止假指纹相同\n  - [ ] 在 `plot_sync` 绘图闭包内通过 `iloc[-count:]` 精细裁剪画图数据\n- [ ] 运行静态语法编译检查，确保 100% 无语法错误\n- [ ] 重启 Flet 主程序，启动盘面和 AI 状态面板的实盘测试\n- [ ] 校验 M1 周期自动数据放大与绘图裁剪的视觉表现，确保不拥挤且正常研判\n"