{
  "AllowMultiple": "false",
  "Description": "\"彻底重构 draw_kline_img 行情刷新与 AI 直连更新链路，破除行情自动更新死锁，实现 Tick 秒级自动刷新，并优化 M1 数据自适应放大及裁剪绘图。\"",
  "EndLine": "1490",
  "Instruction": "\"重构 draw_kline_img 方法。将 AI 计算与 UI 状态更新移至哈希过滤拦截的前方，避免哈希相等导致 AI 不更新。在哈希指纹中加入 len(df) 防止同步假指纹拦截。针对 M1 周期自适应放大拉取 K 线数量。在 Matplotlib 画图时裁剪数据，保持 K 线图视觉整洁不拥挤。\"",
  "ReplacementContent": "\"    async def draw_kline_img(self, force=False, defer_update=False):\\n        if self.is_drawing:\\n            return\\n        now_ts = time.time()\\n        if not force and now_ts - self.last_chart_render_at < self.chart_refresh_interval:\\n            return\\n        self.is_drawing = True\\n        try:\\n            win_w = self.page.width if (self.page and self.page.width) else 1350\\n            win_h = self.page.height if (self.page and self.page.height) else 850\\n            pw = max(600, win_w - 320)\\n            ph = max(300, win_h - 400)\\n\\n            count = max(self.min_bar_count, self.bar_count)\\n            offset = max(0, self.chart_offset)\\n            \\n            # --- M1 周期数据量自适应放大，破除 5 倍重采样导致的 K 线萎缩死锁 ---\\n            resample_group = 5 if str(self.current_tf).upper() == \\\"M1\\\" else 1\\n            fetch_count = count\\n            if resample_group > 1 and offset == 0:\\n                fetch_count = max(300, count * resample_group)\\n\\n            self.chart_range_text.value = f\\\"{count} 根K线 | 偏移 {offset}\\\"\\n            df = await asyncio.to_thread(\\n                data_engine.get_market_data,\\n                self.current_symbol,\\n                self.current_tf,\\n                fetch_count,\\n                offset,\\n            )\\n            if df is None or len(df) == 0:\\n                return\\n\\n            # --- [前置 AI 决策分析链路] 确保在任何 Tick 跳动下 100% 自动秒级刷新数值，绝不受绘图拦截阻碍 ---\\n            dynamic_lookback = max(30, len(df) // resample_group)\\n            gate_overrides = {\\n                \\\"major_pivot_max_age_bars\\\": int(dynamic_lookback)\\n            }\\n            curve_signal = await asyncio.to_thread(\\n                curve_analysis_service.analyze_dataframe,\\n                df,\\n                lookback=dynamic_lookback,\\n                smooth_window=5,\\n                point=0.01,\\n                cache_ttl=1.5,\\n                resample_group=resample_group,\\\n<truncated 6294 bytes>",
  "StartLine": "1344",
  "TargetContent": "\"    async def draw_kline_img(self, force=False, defer_update=False):\\n        if self.is_drawing:\\n            return\\n        now_ts = time.time()\\n        if not force and now_ts - self.last_chart_render_at < self.chart_refresh_interval:\\n            return\\n        self.is_drawing = True\\n        try:\\n            win_w = self.page.width if (self.page and self.page.width) else 1350\\n            win_h = self.page.height if (self.page and self.page.height) else 850\\n            pw = max(600, win_w - 320)\\n            ph = max(300, win_h - 400)\\n\\n            count = max(self.min_bar_count, self.bar_count)\\n            offset = max(0, self.chart_offset)\\n            self.chart_range_text.value = f\\\"{count} 根K线 | 偏移 {offset}\\\"\\n            df = await asyncio.to_thread(\\n                data_engine.get_market_data,\\n                self.current_symbol,\\n                self.current_tf,\\n                count,\\n                offset,\\n            )\\n            if df is None or len(df) == 0:\\n                return\\n\\n            # 计算 K 线数据哈希指纹，检测盘面是否确实变化\\n            latest_bar = df.iloc[-1]\\n            vol_col = \\\"Volume\\\" if \\\"Volume\\\" in df.columns else \\\"tick_volume\\\" if \\\"tick_volume\\\" in df.columns else \\\"volume\\\" if \\\"volume\\\" in df.columns else \\\"\\\"\\n            vol_val = latest_bar[vol_col] if vol_col else 0\\n            close_val = latest_bar[\\\"Close\\\"] if \\\"Close\\\" in df.columns else latest_bar[\\\"close\\\"] if \\\"close\\\" in df.columns else 0\\n            time_val = latest_bar[\\\"time\\\"] if \\\"time\\\" in df.columns else latest_bar[\\\"Time\\\"] if \\\"Time\\\" in df.columns else 0\\n            current_state_hash = (\\n                self.current_symbol,\\n                self.current_tf,\\n                count,\\n                offset,\\n                time_val,\\n                close_val,\\n                vol_val,\\n                pw,\\n                ph\\n            )\\n            \\n            if not force and current_state_hash == getattr(self, \\\"last_chart_hash\\\", \n<truncated 5235 bytes>",
  "TargetFile": "\"e:\\\\python\\\\lh01\\\\gold-quantification\\\\quant_app/ui/app.py\"",
  "toolAction": "\"Replacing app.py code range\"",
  "toolSummary": "\"File edit\""
}