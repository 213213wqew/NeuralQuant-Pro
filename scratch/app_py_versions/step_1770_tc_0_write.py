"# 智能对冲风控系统 — 实现说明\n\n## 完成内容\n\n### 1. Flet 崩溃修复 ✅\n- **问题**：`ft.ElevatedButton(text=\"...\")` 在新版 Flet 中导致 `TypeError` 启动崩溃。\n- **修复**：改用 `content=ft.Row([ft.Icon(...), ft.Text(...)])` 方式，彻底规避参数冲突。\n\n### 2. 新增：智能对冲风控面板 ✅\n在 `build_home_view` 右侧控制面板底部新增了一个专属风控卡片：\n- 🔶 **琥珀色标题**：\"智能对冲风控\" + 副标题说明\n- **深暗橙色背景** (`#1a1209` + `#3d2b0a` 边框)，视觉上与量化看板区分\n- 三个按钮布局：上排 `[一键锁仓] [智能解仓]`，下排独占 `[一键全平]`\n\n### 3. 一键锁仓（`handle_lock_hedge`）✅\n**逻辑**：\n1. 通过 `mt5.positions_get(symbol)` 获取所有持仓\n2. 计算多空净敞口：`net = buy_vol - sell_vol`\n3. 若 `|net| >= 0.01`，开反向对冲单（magic=19999，comment=\"hedge_lock\"）\n4. 调用 `order_executor.place_order()` 异步执行\n5. 结果通过 SnackBar 实时反馈\n\n### 4. 一键全平（`handle_close_all`）✅\n**逻辑**：\n1. 点击后弹出 **AlertDialog 二次确认**（modal=True，防误触）\n2. 确认后调用 `order_executor.close_all_for_symbol(symbol)`\n3. 无 Magic 限制，关闭品种下所有持仓\n4. 执行过程和结果通过 SnackBar 反馈\n\n### 5. 智能解仓（`handle_unlock_hedge`）✅\n弹出 MQL5 解仓助手对话框，展示 4 种专业策略供选择：\n\n| 策略 | 方法 | 真实后端 |\n|------|------|---------|\n| S1 双向 CloseBy 冲抵 | `TRADE_ACTION_CLOSE_BY` | ✅ 真实执行 `close_by_opposite` |\n| S2 盈利冲抵法 | 每赚 $5 部分平仓 | 🔶 提示激活（需策略模块） |\n| S3 区间突破跟踪 | ATR 突破触发 | 🔶 提示激活（需策略模块） |\n| S4 DCA 均价合并 | 1.5倍马丁 DCA | 🔶 提示激活（需策略模块） |\n\n### 6. 文件变更\n\n| 文件 | 变更 |\n|------|------|\n| `quant_app/ui/app.py` | 新增 import、3 个按钮定义、风控面板
<truncated 482 bytes>