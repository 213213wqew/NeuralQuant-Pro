"# 对冲风控系统架构重构方案\n\n## 问题描述\n\n当前实现将所有锁仓/解仓/全平逻辑写在了 `app.py` 的 handler 方法中，架构存在以下问题：\n\n1. **逻辑混在 UI 层** — `_do_lock_hedge`、`_do_close_all`、`_do_unlock_strategy` 属于业务逻辑，不应在 UI 文件中\n2. **无状态持久化** — 程序关闭后不知道当前是否处于\"锁仓状态\"，重新启动无法恢复\n3. **无状态展示** — 控制面板按钮不会反映当前是锁仓/未锁仓状态\n\n---\n\n## 正确的架构设计\n\n```\nquant_app/\n├── modules/\n│   └── hedge/                        ← [NEW] 专门的对冲风控模块\n│       ├── __init__.py\n│       └── hedge_manager.py          ← 核心引擎：锁仓/解仓/全平 + 状态持久化\n├── core/\n│   └── order_executor.py             ← (现有) 底层 MT5 下单，不修改\n└── ui/\n    └── app.py                        ← (修改) 仅保留 UI 按钮 + 状态展示\n```\n\n---\n\n## Open Questions\n\n> [!IMPORTANT]\n> 请确认以下设计决策后再执行：\n\n1. **状态文件路径**：建议存在 `quant_app/data/hedge_state.json`（与项目同目录）。是否有其他偏好路径？\n2. **锁仓状态展示**：控制面板上是否需要显示当前锁仓的具体信息，例如\"🔒 已锁仓 | 对冲单 #xxxxx | 0.50手 | 已锁 02:15\"？\n3. **S2/S3/S4 策略**：盈利冲抵/突破跟踪/DCA合并 这三个策略目前只是显示\"激活提示\"，是否要在这次一起实现后端执行逻辑，还是留给后续策略模块？\n\n---\n\n## Proposed Changes\n\n### [NEW MODULE] `quant_app/modules/hedge/`\n\n#### [NEW] `quant_app/modules/hedge/__init__.py`\n空文件，使其成为 Python 包。\n\n#### [NEW] `quant_app/modules/hedge/hedge_manager.py`\n\n独立的对冲业务引擎，包含：\n\n**状态数据结构（持久化到 JSON）**：\n```json\n{\n  \"is_locked\": true,\n  \"lock_time\": \"2026-05-20T10:15:00\",\n  \"symbol\": \"
<truncated 2201 bytes>