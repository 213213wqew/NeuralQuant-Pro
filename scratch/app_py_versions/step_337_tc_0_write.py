"# 任务清单 (Tasks)\n\n- [x] 在 `build_settings_view` 中为 3 个策略卡片各自初始化专属的微型预设下拉选择器，存放于 `self.preset_selectors` 字典中\n- [x] 移除老旧的全局 `preset_selector` 选择栏布局与提示\n- [x] 修改 `render_strategy_cards` 绘制逻辑：\n  - [x] 将卡片高度调整为自适应 `240`，保证卡片内部控件不被裁切\n  - [x] 针对被选中的策略卡片，在底部内嵌专属的微型发光 `ft.Dropdown` 控件\n  - [x] 针对未选中的策略卡片，在底部显示带有设置图标的灰色只读预设名称小字\n- [x] 重构 `save_settings` 逻辑，精准读取被激活卡片中的预设值并写入 `.env`\n- [x] 简化 `on_strategy_changed` 逻辑，移除多余的全局下拉框更新，仅在策略自动化运行中时实时同步最新的策略和预设\n- [x] 彻底移除旧有的 `confirm_and_run_strategy` 相关的绿色大按钮及多余动作代码\n- [x] 合并重组 Footer 排版，把保存设置按钮与详细说明文档超链接完美融合为底部的平级 Row 布局\n- [x] 编译与验证测试：\n  - [x] 运行 `python -m py_compile quant_app/ui/app.py` 确保 100% 零语法错误\n  - [ ] 确认运行机制无误，完成 Walkthrough 文档更新\n"