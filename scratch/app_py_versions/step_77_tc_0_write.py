"# Implementation Plan - Settings Page Strategy Selector Card View (Fully Chinese)\n\nConvert the dropdown selector for active strategy in the settings view to an interactive premium card-based selection grid, using highly polished styles, micro-animations, and complete Chinese localization for all elements in the settings tab.\n\n## User Review Required\n\n> [!IMPORTANT]\n> - All texts, labels, hints, and descriptions in the settings view will be in **Chinese**.\n> - The new design replaces the old `ft.Dropdown` selection list for `交易策略` with a set of three premium grid cards aligned side-by-side. \n> - To ensure seamless integration without breaking any existing backend state-handling, we introduce a virtual state holder `StrategySelectorState` which mimics the `.value` property.\n> - No other strategy-related logic is affected.\n\n## Proposed Changes\n\n### UI Components\n\n#### [MODIFY] [app.py](file:///e:/python/lh01/gold-quantification/quant_app/ui/app.py)\n\n- **Define `StrategySelectorState`**: A simple class at module-level in `app.py` to hold the selected strategy value, maintaining full backward compatibility.\n- **Implement card rendering logic**: Add `render_strategy_cards` and `select_strategy_card` methods to `AQuantApp`.\n  - Draw 3 premium, highly interactive cards in 100% Chinese (经典网格马丁 V1, 趋势波段拐点 V2, 自适应智能网格 V4).\n  - Use animations (`animate`) for smooth border and background transitions.\n  - Display checkmark icon overlays in the top right corner of the selected card.\n- **Update `build_settings_view`**:\n  - Ensure all labels, texts, and tooltips are in Chinese.\n  - Replace the dropdown strategy selector with the new `self.strategy_cards_container` layout.\n  - Group and organize settings elements (MT5 path, Strategy selection cards, Preset dropdown + current preset labels) in clean rows and columns.\n\n## Verification Plan\n\n### Manual Verification\n- Launch the application using `启动桌面专业版.bat`.\n- Open the \"设置\" (Settings)
<truncated 592 bytes>