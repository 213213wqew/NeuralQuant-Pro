{
  "Description": "\"在 update_ui_data 方法中添加对 AI 评分与 AI 背景的 UI 实机信号兜底提取，同时新增对新重构的环形百分比仪表盘图表的同步数据更新。\"",
  "Instruction": "\"在 update_ui_data 行情刷新循环中增加 AI 分数、背景的 UI 缓存兜底，并增加全新环形 ProgressRing 图表的数据更新\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":2042,\"ReplacementContent\":\"        # --- 更新 AI 评分与微型能量条 ---\\n        risk_score = 50.0\\n        dir_score = 50.0\\n        if hasattr(strategy_runner, 'current_strategy') and strategy_runner.current_strategy is not None:\\n            risk_score = getattr(strategy_runner.current_strategy, 'latest_risk_score', 50.0)\\n            dir_score = getattr(strategy_runner.current_strategy, 'latest_dir_score', 50.0)\\n            \\n        # 兜底：如果策略返回默认值 50.0，则优先读取 UI 实时缓存分，让 AI 风险和动能数据动起来！\\n        if risk_score == 50.0 and hasattr(self, 'latest_risk_score'):\\n            risk_score = self.latest_risk_score\\n        if dir_score == 50.0 and hasattr(self, 'latest_dir_score'):\\n            dir_score = self.latest_dir_score\",\"StartLine\":2039,\"TargetContent\":\"        # --- 更新 AI 评分与微型能量条 ---\\n        risk_score = getattr(strategy_runner.current_strategy, 'latest_risk_score', 50.0)\\n        dir_score = getattr(strategy_runner.current_strategy, 'latest_dir_score', 50.0)\"},{\"AllowMultiple\":false,\"EndLine\":2140,\"ReplacementContent\":\"        # --- 更新多周期背景显示 ---\\n        ai_phase = \\\"---\\\"\\n        if hasattr(strategy_runner, 'current_strategy') and strategy_runner.current_strategy is not None:\\n            ai_phase = getattr(strategy_runner.current_strategy, 'latest_ai_phase', \\\"---\\\")\\n        if ai_phase == \\\"---\\\" and hasattr(self, 'latest_ai_phase'):\\n            ai_phase = self.latest_ai_phase\\n        self.ai_phase_text.value = f\\\"背景: {ai_phase}\\\"\",\"StartLine\":2137,\"TargetContent\":\"        # --- 更新多周期背景显示 ---\\n        ai_phase = getattr(strategy_runner.current_strategy, 'latest_ai_phase', \\\"---\\\")\\n        self.ai_phase_text.value = f\\\"背景: {ai_phase}\\\"\"},{\"AllowMultiple\":false,\"EndLine\":2157,\"ReplacementContent\":\"        # --- 动态计算并更新 AI 因子能量条 (采用稳定映射逻辑，绝不卡顿) ---\\n        trend_val = min(max(\n<truncated 2430 bytes>",
  "TargetFile": "\"e:\\\\python\\\\lh01\\\\gold-quantification\\\\quant_app\\\\ui\\\\app.py\"",
  "toolAction": "\"更新仪表盘数据和AI兜底数据\"",
  "toolSummary": "\"更新仪表盘数据和AI兜底数据\""
}