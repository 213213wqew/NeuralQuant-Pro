"# AI 因子图表化与风控直连重构任务清单 (Tasks)\n\n- `[/]` 环形百分比仪表盘图表重构\n  - `[ ]` 封装 `_make_factor_gauge` 原生组件构建器\n  - `[ ]` 修改 `build_home_view` 将原本的静态因子进度条重构为横向 ProgressRing 并排布局\n- `[/]` AI 实机分析引擎风控直连\n  - `[ ]` 在 `draw_kline_img` 异步行情刷新中接入 `curve_signal` 风控源，并换算为 AI 方向分、AI 风险分与背景研判\n  - `[ ]` 在 `update_ui_data` 行情更新中实现无缝兜底读取，同步更新原生进度条及全新 ProgressRing 指标\n- `[/]` 验证与部署\n  - `[ ]` 静态编译 `py_compile` 校验，确保修改后 100% 语法无错\n  - `[ ]` 杀掉当前 `task-851` 背景进程\n  - `[ ]` 重新拉起启动脚本进行实盘运行验证，观测环形图仪表盘霓虹特效及 AI 风险分跳动情况\n"